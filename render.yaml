services:
  - type: web
    name: cs2-prop-scanner
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python webapp.py
    healthCheckPath: /api/status
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
